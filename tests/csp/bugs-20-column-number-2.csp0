# The percent sign should cause a parse error.  In Bison 2.3, column
# numbers start at 0, whereas we want them to start at 1.  This test
# case ensures that we get correct error message output when the parse
# error occurs after the first line.

%
