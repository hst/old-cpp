Release 1.0-α1:

  • Traces refinement
  • Operators:
      prefix (→), external choice (□), internal choice (⊓), interrupt
      (▵), sequential composition (;), interleaving (|||),
      alphabetized parallel ([αP‖αQ]), interface parallel (〚α〛),
      hiding (\), renaming (〚μ〛)
